# W4LLET Fullstack App

This repo contains both the frontend (Vite + React) and backend (NestJS + MongoDB) of the W4LLET app.

## 📦 Installation

1. Clone the repository
2. Run:

```bash
npm run install-all
npm run dev
```
